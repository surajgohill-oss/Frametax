# Canonicalized new-program optimizer audit — Codex

## Gate

**STATUS: PARTIAL — runtime delta localized; production remediation remains.**

The current canonical corpus contains 586 source identities. The independent new-program delta is exactly 285 records (119 AG-only and 166 Codex-only); the reconciled 20-material-conflict cohort remains frozen. Normalization derives five intended active formulaic survivors. Three reproduce their base economics through the real DB-backed evaluation, persistence and reuse path. DAVE correctly fails closed without its narrower labour/activity base but lacks a complete identity/input binding. Ohio is not executable. Nevada reproduces its 15% base case but its own doctrine records material unmodeled constraints.

## 53-warning normalization

| Classification | Count |
|---|---:|
| FALSE_POSITIVE_ALIAS | 2 |
| FALSE_POSITIVE_HISTORICAL_ID | 2 |
| FALSE_POSITIVE_AUTHORITY_VETOED | 31 |
| EXPECTED_CONDITIONAL_PATH | 13 |
| REAL_REACHABLE_STALE_PRICING_PATH | 0 |
| REAL_MISSING_EXECUTABLE_BINDING | 3 |
| REAL_MISSING_PIPELINE_PROOF | 2 |
| CURRENT_CANONICAL_STATE_CONFLICT | 0 |

The two `REAL_MISSING_PIPELINE_PROOF` warnings (Bulgaria and Malaysia) are closed by the new DB-backed harness. Nevada's prior proof warning normalized instead to `REAL_MISSING_EXECUTABLE_BINDING` because the base path passes while its documented cap, ratio, and personnel-basis rules do not exist in the evaluator.

## Derived active population

| Surviving program | Result |
|---|---|
| `bg_film_encouragement_act_rebate` | USD 2m × 25% = USD 500k; persisted/reused; manifest/runtime provenance disposition conflicts. |
| `ca_bc_dave` | Candidate generated; an auditable unpriced result with null incentive persists and reuses until qualified BC labour/activity base exists. |
| `my_finas_rebate` | USD 2m × 30% = USD 600k; persisted/reused; cultural uplift remains conditional. |
| `proposed_united_states_ohio_ohio_motion_picture_tax_credit` | No US-OH seed, doctrine, rate or discovery binding. |
| `us_nv_film_credit` | USD 2m × 15% = USD 300k; persisted/reused; material cap/ratio/personnel rules remain unmodeled. |

No member of this five-program active delta is named in the current stack-pair registry or treaty-unlock registry. DAVE is a component opportunity, but no automatic stack permission is inferred; it remains safely unpriced without its narrower base.

## Independent-oracle limits

The harness uses literal expected values rather than importing production rates. It proves qualifying base arithmetic and reuse for Bulgaria, Malaysia, and Nevada, and negative fail-closed behavior for DAVE and Ohio. It does not claim a positive DAVE calculation, an Ohio calculation, or a Nevada high-value cap boundary. Those are remediation acceptance conditions, not inferred passes.

## True remaining cleanup

Four precise rows remain in `CODEX_TRUE_REMAINING_CLEANUP_MANIFEST.csv`: one controlling-provenance conflict (Bulgaria), two missing program/input bindings (DAVE and Ohio), and one partial economic binding (Nevada). The 31 authority-vetoed rate records, 13 expected conditional/display paths, two aliases, and two historical/component identities are not production defects.

## Frozen scope

Netherlands, South Africa, Oregon, the original single-jurisdiction algorithm, MFNI, reinvestment, and UI were not reopened. No external research or production code change was performed.
