# Global program Claude wiring audit — Codex

## Decision

Status: **READY FOR CLAUDE BOUNDED REMEDIATION**. Final runtime acceptance is **not** granted. The accepted 586-row research universe is intact, but Claude's partial wiring is catalog visibility rather than new deterministic economic consumption, and 107 exact follow-up items remain.

## Independently reproduced counts

| Measure | Count |
|---|---:|
| Accepted manifest rows | 586 |
| Claude result rows | 586 |
| Pre-existing `ALL_PROGRAMS` entries | 303 |
| Claude-appended entries | 164 |
| Current `ALL_PROGRAMS` entries | 467 |
| Claude remaining rows | 146 |
| B1 rows | 46 |
| B2 rows | 6 |
| B3 rows | 17 |
| Insufficient-authority rows omitted from the stated 70 | 77 |
| B4 code path (not a CSV row) | 1 |

The 146-row equation is `46 + 6 + 17 + 77 = 146`. B4 is an additional systemic code-path defect. Of the 77 insufficient-authority rows, 42 already use pre-existing priced rules and contain no accepted corrective fact; 35 are already non-priced. Under the no-new-research boundary, those are respectively retained and correctly non-priced, not wiring blockers.

The 467-versus-586 relationship is not a 119-program shortfall: the two sets are different identity layers. Current `ALL_PROGRAMS` is exactly 303 pre-existing catalog objects plus 164 appended objects. The 586 accepted rows also include aliases, retired/evidence identities, doctrine/rate-rule identities, and explicit non-entry dispositions, while the older 303-entry catalog contains legacy aggregate records that are not independent accepted-program rows. The reconciliation CSV therefore maps each accepted row to an existing ID, an appended catalog object, an alias target, or an explicit non-entry disposition instead of subtracting the two totals. Forty-two Claude appends still violate that mapping and are the bounded identity-cleanup work.

## Row-level classification

| Classification | Count |
|---|---:|
| FULLY_WIRED | 56 |
| CORRECTLY_NON_PRICED | 422 |
| ALIAS_RESOLVED | 1 |
| RETIRED | 1 |
| BLOCKED_B1 | 46 |
| BLOCKED_B2 | 6 |
| BLOCKED_B3 | 12 |
| OTHER_BLOCKER | 42 |
| **Total** | **586** |

The complete row mapping is in `GLOBAL_PROGRAM_WIRING_REMAINING_RECONCILIATION_CODEX.csv`.

## Claude implementation audit

All 164 appended objects have `program_slug=None`, so they cannot resolve through the deterministic rate registry. They carry provenance/reason text and can surface as catalog or conditional discovery, but none proves a newly wired formulaic economic rule. Conditional-program economics are intentionally zero and do not enter NPC/ranking.

The append is not identity-clean. It includes all 13 `MERGE_ALIAS` rows and one `RETIRE` row as fresh catalog entries even though those actions require non-entry/identity consolidation. It also contains 28 same-jurisdiction same-program collisions visible directly against the prior 303-entry catalog. These 42 rows are enumerated as `OTHER_CATALOG_IDENTITY` work items; the remedy is identity merge/removal of the duplicate append, never deletion of the surviving program and never creation of a second priceable identity. Intentional compound source identities already marked fail-closed in the accepted alias-lineage artifact are not silently promoted.

## Binding blocker decisions

- **B1:** 13 programs are display-only with zero guaranteed value; 33 fail closed. No current separate probability-adjusted lane exists, so no discretionary expected value may reduce guaranteed NPC. Old tests demanding deterministic pricing are superseded; zero-NPC and refusal tests remain valid.
- **B2:** California Program 4.0 survives as `ca_film_30`, with `us_ca_film_credit` as a compatibility alias. New York's main credit survives as `ny_state_film`/legacy `us_ny_film_credit`; the post-production credit stays a distinct fail-closed identity. Ontario OPSTC survives as `on_opstc`, with `ca_on_opstc` and the evidence-only duplicate normalized to it.
- **B3:** Five of 17 close without new wiring (`bc_pstc`, `ca_federal_cptc`, `mu_edb_incentive`, `us_la_film_incentive`, and retirement of the false separate Iceland post identity). Twelve require the exact bounded changes in the rate-rule spec.
- **B4:** fail-closed authority status must actively preempt rate and stack resolution. Missing `program_slug` is not a safety gate. The exact precedence and adversarial tests are in the gate specification.

The named correction checks reconcile as follows: BC PSTC is already correctly wired while the PSTC/DAVE compound catalog identity must remain non-priced; Australia Location Offset is B3 and Queensland PDV is B1 fail-closed; Germany DFFF is B1 display-only zero guaranteed under the accepted manifest; Czech, Iceland national reimbursement, Netherlands NFPI, Thailand, Texas MIIP, and South Africa NFVF are specified row-by-row in the B3 artifact. South Africa's separate DTIC foreign-film identity remains B1 fail-closed. No named item was omitted merely because it sits outside the 17-row B3 subset.

## Four-project runtime interpretation

Fresh targeted runtime inspection produced 347 Little Utopia, 444 F#K Valentine's Day, 344 Bad Hombres, and 446 Lips Like Sugar structures. LU's Mauritius baseline priced at $573,059.70 incentive / $3,791,333.30 NPC but did not become Recommended because its role qualification is `AUTHORITY_UNRESOLVED`. F#K's Greece baseline priced at $1,445,659.84 / $3,072,027.16 but remains `USER_FACT_REQUIRED` for the aggregate cultural test. Bad Hombres and Lips produced Recommended baselines. These are pre-existing qualification outcomes, not proof that Claude's 164 catalog additions are economically consumed.

Lips currently uses legacy `us_ca_film_credit`; bounded remediation must preserve its economics while canonicalizing that slug to `ca_film_30`.

## Acceptance boundary

Claude may implement only `GLOBAL_PROGRAM_CLAUDE_FOLLOWUP_MANIFEST_CODEX.csv`. No broad research, MFNI work, optimizer/UI redesign, or unrelated rule edits are authorized. After that commit, Codex must independently rerun targeted acceptance across all four projects. Until then, final runtime acceptance remains **NO**.
