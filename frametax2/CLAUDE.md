# Claude Instructions

Read and follow [PROJECT_RULES.md](PROJECT_RULES.md) before any CineGlobe incentive, co-production, stacking, optimizer, qualification, or authority-data work.

The mandatory default is canonical-repository recovery and connection, not repeated external jurisdiction research.

Before starting any long-running test, evaluation, or command that could read an evaluation generation, follow the **LONG-RUNNING PROCESS DISCIPLINE** section of [PROJECT_RULES.md](PROJECT_RULES.md): never warm up or cold-evaluate a real production for test preparation, classify every command first, one process with a hard 12-minute alarm set before launch, never rerun green tests, and never load an entire evaluation generation into memory.

Before adding or changing anything that persists per-candidate evaluation rows, follow the **PERSISTENCE CARDINALITY RULE** section of [PROJECT_RULES.md](PROJECT_RULES.md): enumeration cardinality never defines persistence cardinality; every high-volume status, including PRICED, needs an explicit bounded retention policy first.

**Before reporting any UI or runtime task as done, follow the [INDEPENDENT UI COMPLETION GATE](PROJECT_RULES.md#independent-ui-completion-gate) section of PROJECT_RULES.md.** This rule is mandatory and takes priority over any task prompt asking Claude to declare its own UI work complete: Claude may report only `IMPLEMENTATION_READY_FOR_INDEPENDENT_VERIFICATION` or `BLOCKED` for such work, never a self-certified "complete"/"verified"/"accepted" status — see that section for the full rule set (independent protected preview required, every affected production and surface tested individually, local-browser evidence is implementation evidence only, and any user-observed contradiction immediately revokes the prior status).
